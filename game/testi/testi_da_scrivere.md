# Testi da scrivere

Stato al 10 agosto 2026. Ogni voce va riempita in `game_data.js`, nel campo
indicato: qui si tiene solo l'elenco.

## Hotspot di tipo `testo` con `testo: ''` (10)

Toccandoli oggi si apre un toast **completamente vuoto**. Sono le frasi di
scenario che dicono «qui non c'è niente» senza ripetersi: servono per fare
atmosfera e per non far sembrare rotto il gioco.

`s2-T2` · `s3-T1` · `s3-T2` · `s3-T3` · `s4-T1` · `s4-T2` · `d1-T1` · `d1-T2` ·
`d2-T1` · `d2-T2`

(Già scritti, come misura del tono: `s1-T1` «Qui non c'è niente.», `s1-T2`
«Continua a cercare.», `s2-T1`.)

## Oggetto senza nome (1)

`s3-I1` è l'unico oggetto raccoglibile del gioco e ha `nome: ''` e
`descrizione: ''`: entra nello zaino anonimo, e il toast che lo offre è senza
titolo né testo. Servono nome e descrizione.

## Piume senza descrizione (7)

`s1-F1` · `s2-F1` · `s2-F2` · `s3-F1` · `s3-F2` · `s4-F1` · `d2-F1`.
Non è urgente: il motore ripiega su `piumaDescrizione` in `GAME_DATA.testi`.

## Teaser degli enigmi (14)

Vedi `teaser_enigmi.md`: oggi non sono mostrati da nessuna parte. Vanno riscritti
solo se si decide di rimetterli in gioco.
